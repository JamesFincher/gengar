#!/usr/bin/env python3
"""
Gengar CLI launcher.

This wrapper should behave like the installed `gengar` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hermes_cli.main import main
    main()
